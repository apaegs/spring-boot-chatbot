ai.api-key=test-key
ai.model=test-model
ai.base-url=http://localhost:${wiremock.server.port}
ai.retry.max-retries=3
ai.retry.delay=100
ai.retry.multiplier=2
conversation.session.max-size=100
conversation.session.expire-minutes=30
conversation.session.max-history=10
